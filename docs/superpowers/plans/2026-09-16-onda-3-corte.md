# Onda 3 com o contrato CORTE — cada etapa é um PR (16/09/2026, noite)

Contrato: `docs/briefs/design/DESIGN.md` (direção **CORTE**, escolhida em
`DIRECAO-ESCOLHIDA.md`, veto do dono pendente). O gate é o CI
(`.github/workflows/suite.yml`) e `main` só recebe PR — cada etapa abaixo
nasce numa branch, passa pelo check "suíte completa", faz merge pela API
(`scripts/github.py`), é provada em produção por `/saude/` (commit) e por
QA nos dois regimes, com o demo intacto.

## Global Constraints

- Tudo em token; as catracas de valor cru (`config/test_design_system.py`)
  só descem. `:has()` proibido. Alvo 44 × 44. Texto ≥ 11 px.
- AA em TODO par nos dois regimes: `config/tests.py` + os 274 pares de
  `scripts/qa/auditar_contraste.py` antes de cada PR.
- `prefers-reduced-motion` desliga tudo; keyframe nasce com consumidor.
- Nenhum hex fora das listas `--ferro-*` / `--papel-*` (`config/test_ferro.py`).
- Sprite: 13 símbolos, traço 2 em cada `<symbol>`, fora do shell offline.
- Segredo nunca no repositório; `artifacts/` e `scratchpad/` nunca no git.
- Ledger compartilhado (`C:\Users\biel-\nutriplan-ledger.md`) antes de
  tocar `CLAUDE.md`, `BACKLOG.md`, `scripts/hooks/*`, `push/*`, `templates/base.html`.

## As etapas

| PR | branch | escopo | prova |
|---|---|---|---|
| 2 | `design/mesa-e-ferro` | a fundação (onda 3 da noite, foco, sprite, catracas, hook, contrato CORTE, referências) | CI; `/saude/`; smoke |
| 3 | `design/corte-tokens` | **T3.1′** — Ferro é a base no `:root` (`--x: var(--ferro-x)`), Papel derivado em `@media (prefers-color-scheme: light)`, `body.modo-foco` força Ferro; valores da tabela do DESIGN.md; `--corte`/`--corte-g`/`--corte-p` declarados; `--shadow-*`/`--edge` = `none`; `--mov-recompensa`; manifesto/theme-color acompanham | `config/test_ferro.py` invertido; `config/tests.py` mede os dois regimes pelos novos âncoras; `test_tema_claro.py` vira "o Ferro é a base"; auditoria 274 pares; QA 390/320 × 2 regimes em Entrar/Hoje/ficha/execução/progresso |
| 4 | `design/corte-fontes` | **T3.2** — Bodoni Moda (display, ≥ 20 px, só herói) + Karla (texto, `tabular-nums`) auto-hospedadas em `static/fonts/` (woff2 latin), `@font-face` com `font-display: swap`, `--fonte-display`/`--fonte-texto`, precache no `sw.js` + `push/assets.py`, os quatro pesos; gate ≤ 260 KB | teste do teto de bytes e da licença; `sw.js` × `assets.py`; `document.fonts.check` no QA; nenhum `font-weight` fora dos quatro |
| 5 | `design/corte-componentes` | **T3.3 + T3.4** — cartão → faixa (sem sombra, `--surface` de borda a borda), prato com `--corte-g`, lista/botão/campo/chip/mídia com `--corte`, chip pequeno `--corte-p`, `--pill` só em ponto/selo; `.btn` (primário lima cheia, ghost `--fio-forte`, quiet, perigo) com `--corte`; tiles com número herói | teste de identidade dos raios (substitui 24/16/12/8); `--pill` proibido em clicável; `.card` sem sombra; QA vitrine + cinco telas × 2 regimes |
| 6 | `design/corte-execucao` | **T3.5 + T3.6** — campo (`--surface-2`, `--corte`, rádio/caixa como folha, sufixo de unidade), `.choice-list--dias → .segmented--envolve`; tabbar com a aba ativa como folha cheia de `--brand`; **frame de recompensa** na última série (`folha-sobe`, `--mov-recompensa`, número DENTRO da folha, cascata 80 ms), `corte-abre` ao registrar refeição, `corte-desdobra` ao bater meta; emoji → glifo nas conquistas | keyframes com consumidor; reduced-motion; QA da execução até a última série com o demo; contraste do frame |

Depois de cada merge: rebase da próxima branch sobre `main` (a proteção é
`strict`), `/saude/` mostrando o merge commit, smoke (`/`, `/conta/entrar/`,
`/demo/`, `/saude/`, estáticos), QA em produção nos dois regimes com
`scripts/qa/nav.py` (público + demo; conta descartável pelo signup é passo
do dono — ver relatório).

## Registro

`CLAUDE.md` "Design / Onda 3" (só essa seção), `docs/sistema-visual.md`,
plano mestre (T3.1–T3.7 apontando para cá), BACKLOG "Design / Onda 3".
